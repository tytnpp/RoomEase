<div class="max-w-5xl mx-auto mt-6 space-y-6 bg-gray-50 p-6 rounded-lg shadow-lg">
  <% @rooms.each do |room| %>
    <%= link_to room_path(room), class: "block bg-white shadow-lg rounded-2xl p-4 border border-gray-300 hover:shadow-xl transition duration-300" do %>
      <div class="mt-4 flex justify-between items-center">
        <h2 class="text-xl font-semibold text-gray-900 hover:underline"><%= room.name %></h2>
        <span class="text-gray-500 text-sm">Capacity: <%= room.capacity %></span>
      </div>

      <div class="text-yellow-500 mt-1">
        ⭐⭐⭐⭐⭐ <span class="text-gray-500 text-sm">(256 Reviews)</span>
      </div>

      <p class="text-gray-500 text-sm mt-2">0.7 km to the center</p>
    <% end %>
  <% end %>
</div>

<!--<div class="max-w-5xl mx-auto mt-6 space-y-6 bg-gray-50 p-6 rounded-lg shadow-lg">-->
  <%# @rooms.each do |room| %>
    <!-- Room Card -->
<!--    <div 
      class="block bg-white shadow-lg rounded-2xl p-4 border border-gray-300 hover:shadow-xl transition duration-300 cursor-pointer"
      onclick="openModal(<%#= room.id %>)">
      <div class="mt-4 flex justify-between items-center">
        <h2 class="text-xl font-semibold text-gray-900 hover:underline"><%#= room.name %></h2>
        <span class="text-gray-500 text-sm">Capacity: <%#= room.capacity %></span>
      </div>

      <div class="text-yellow-500 mt-1">
        ⭐⭐⭐⭐⭐ <span class="text-gray-500 text-sm">(256 Reviews)</span>
      </div>

      <p class="text-gray-500 text-sm mt-2">0.7 km to the center</p>
    </div>
  <%# end %>-->
<!--</div>

<!-- Modal -->
<!--<div id="reservationModal" class="fixed inset-0 bg-gray-800 bg-opacity-50 hidden flex items-center justify-center">
  <div class="bg-white rounded-lg shadow-lg p-6 w-1/3">
    <h2 class="text-xl font-bold mb-4">Reserve Room</h2>
    <div id="modalContent">
      <!-- Reservation form will be loaded here -->
<!--    </div>
    <button onclick="closeModal()" class="mt-4 bg-red-500 text-white px-4 py-2 rounded">Close</button>
  </div>
</div>

<script>
  function openModal(roomId) {
    // Show the modal
    document.getElementById('reservationModal').classList.remove('hidden');

    // Fetch the reservation form via AJAX
    fetch(`/rooms/${roomId}/reservations/new`)
      .then(response => response.text())
      .then(html => {
        document.getElementById('modalContent').innerHTML = html;
      });
  }

  function closeModal() {
    // Hide the modal
    document.getElementById('reservationModal').classList.add('hidden');
    document.getElementById('modalContent').innerHTML = ''; // Clear modal content
  }
</script>-->